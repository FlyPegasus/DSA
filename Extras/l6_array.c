#include<stdio.h>
int main()
{
    int n,i,j;
    printf("Length of array: ");
    scanf("%d",&n);
    int arr[n];
    for (i = 0; i < n; i++)
    {
        printf("Enter elements of array: ");
        scanf("%d",&(arr[i]));
    }
    for (j = n-1; j <= 0; j--)
    {
        int rev = 0;
    }
    return 0;
}